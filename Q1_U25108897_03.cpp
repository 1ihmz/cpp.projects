#include <iostream>
using namespace std;
int main()
{
int n, k;
cout << "Enter n (Tribonacci term) and k (divisor): ";
cin >> n >> k;
if(n>=1 && k>1){


}
    else
    cout << "Invalid input";
    return 0;
}